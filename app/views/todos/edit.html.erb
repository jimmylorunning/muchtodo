<%= form_for(@todo) do |f| %>
	<%= f.label :task %>
	<%= f.text_field :task %><br />

	<%= f.label :priority %>
	<%= f.select :priority, @priorities %><br />

	<%= f.label :status %>
	<%= f.select :status, @statuses %><br />

	<%= f.submit "Update" %>
<% end %>