Recently Done page
<ul>
<% @todos.each do |todo|  %>
	<div id="<%= todo.task.gsub(/\s+/, '') %>">
	<li><%= todo.task %></li>
	</div>
<% end %>
</ul>
<%= link_to "Back to To Do list", todos_path %>
<%= debug(params) if Rails.env.development? %>